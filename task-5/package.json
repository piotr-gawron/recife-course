{
  "name": "task-5-recife-course",
  "version": "0.0.1",
  "description": "output current weather in Recife",
  "main": "src/index.js",
  "scripts": {
    "build": "browserify src/index.js --s getWeather -o dist/task5-dist.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Piotr Gawron",
  "license": "ISC",
  "dependencies": {
    "request": "^2.79.0"
  }
}
